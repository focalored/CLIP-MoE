# https://wandb.ai
wandb:
  _target_: lightning.pytorch.loggers.wandb.WandbLogger
  project: "multi-index-retrieval"
  tags: ${task_name}
  group: ""
  log_model: False
  save_dir: "${paths.output_dir}"
